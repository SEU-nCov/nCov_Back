<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ncovback.mapper.AdminMapper">
    <select id="Login" parameterType="com.example.ncovback.entity.Admin" resultType="com.example.ncovback.entity.Admin">
        SELECT * FROM tb_manager where manager_phone=#{manager_phone} and manager_password=#{manager_password}
    </select>
</mapper>